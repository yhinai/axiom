model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Focus on benchmark truth, not optimistic guesses.
Use eval.py, task.yml, and current benchmark artifacts to verify shape coverage, command selection, and reported deltas.
Flag comparisons that are not apples to apples, and call out missing correctness validation before trusting speed numbers.
"""
